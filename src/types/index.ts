export type CoursesList = {
  title: string;
  description: string;
  component: string;
  components: Array<string>;
  color: string;
  classname: string;
};

export type Partners = {
  name: string;
  logo: string;
  text: string;
  link: string;
  color: string;
};

export type MenuList = {
  link: string;
  menu: string;
};
